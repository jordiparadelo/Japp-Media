const GoogleCalendarWidget: React.FC<{ calendarId: string }> = ({ calendarId }) => (
  // Implement Google Calendar booking widget
  <div>Google Calendar Booking Widget for {calendarId}</div>
);

export default GoogleCalendarWidget;